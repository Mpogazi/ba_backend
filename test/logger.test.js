// var expect = require('chai').expect;
// var fs     = require('fs');
// var logger = require('../utils/logger');

// describe('Testing the Logging Utils', function() {
//     var levels = logger.logLevels;
//     it('should create a file and write the fail message on it', function() {
//         var tester = 'logger.test.js'
//         var message = 'Call to something failed'
//         logger.logIssue(message, tester, levels.DEBUG, function(success, error) {
//             expect(error).to.be.null;
//         });
//     });
// });

